"use client";

// No random values — deterministic for SSR
export default function ParticleBackground() {
  // Static subtle dots for white theme - no randomness
  return null; // White theme doesn't need particle BG
}
